{
  "name": "node-express-heroku-deployment-guide",
  "version": "1.0.0",
  "description": "Simple guide deploying a simple server to Heroku.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.16.2"
  }
}
